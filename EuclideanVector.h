#ifndef EVEC_H
#define EVEC_H

namespace evec{
	class EuclideanVector{
		private:
			
		public:
			EuclideanVector(unsigned int dimensions);
			EuclideanVector(unsigned int dimensions, double magnitude);
			EuclideanVector(
	};
} //evec

#endif
